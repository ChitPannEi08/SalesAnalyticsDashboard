services:
  - type: web
    name: sales-analytics-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn dashboardapp:server
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
